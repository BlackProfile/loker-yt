"use client";

// STUB — akan digantikan implementasi pusat notifikasi (Task 20-d).
export function NotificationBell({ onOpenTasks }: { onOpenTasks?: () => void }) {
  void onOpenTasks;
  return null;
}
